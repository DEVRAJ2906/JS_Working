//Create 3 divs with common class name - "box". Access them & add some unique text to each of them.

let divs = document.querySelectorAll(".box");

divs[0].innerHTML = "<h1>This is first Div.</h1>";
divs[1].innerHTML = "<h1>This is second Div.</h1>";
divs[2].innerHTML = "<h1>This is third Div.</h1>";
